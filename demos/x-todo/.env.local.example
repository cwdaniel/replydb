# X TODO Demo Configuration
# Copy this file to .env.local and fill in your values

# The Tweet ID to use as the database thread
# This is the numeric ID of the tweet (from the URL or API)
TWEET_ID=1234567890123456789

# Bearer Token for reading (App-only authentication)
# Get this from the X Developer Portal: https://developer.x.com/
BEARER_TOKEN=your_bearer_token_here

# OAuth 2.0 Access Token for writing (User Context authentication)
# Required to post replies. Must have tweet.read and tweet.write scopes.
OAUTH_ACCESS_TOKEN=your_oauth_access_token_here

# Optional: Search tier (recent or all)
# - recent: Last 7 days (available on all tiers)
# - all: Full archive (requires Pro or Enterprise tier)
# SEARCH_TIER=recent
